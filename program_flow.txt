X UserInput takes initial input and checks it for glaring errors (null, non-string entry) if passes sets input to UserInput OBJECT
	
X InputParser ABSTRACT_OBJECT_OR_METHOD takes each line from UserInput OBJECT and puts it into an UserInput.inputArray ( .split(\n))

x Have: Array of Strings
InputMatcher evaluates each index of inputArray (loop)


create a Batch object and put a new Transaction object for each 
	
CALCULATOR_OBJECT takes each Transaction object, clones it, and mutates the price based on keywords and puts it the Batch object 
	
Modified Batch Object is printed out for user.